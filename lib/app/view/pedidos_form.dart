import 'package:flutter/material.dart';

class PedidosForm extends StatelessWidget {
  //const PedidosForm({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Menu de Pedidos'),
      ),
    );
  }
}
